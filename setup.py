from setuptools import setup

setup(name='csvdumper',
      version='0.1',
      description='Dump dicts and arrays to CSV files or strings, in a similar way to the JSON package.',
      url='http://github.com/sodle/py_csvdumper',
      author='Scott Odle',
      author_email='scott@sjodle.com',
      license='MIT',
      packages=['csvdumper'],
      zip_safe=False)
